<main>
  <section class='bg-gray-100 py-10'>
    <div class="max-w-6xl mx-auto">
      <p class="text-center mb-1"><a href="">分類</a></p>
      <h1 class="text-center mb-3"><%= @project.title %></h1>
      <p class="text-center mb-6">提案者<a href="" class="text-blue-400 hover:text-blue-300"><%= @project.organizer %></a></p>
    </div>
  </section>
  <div class="lg:flex justify-between px-12 border-t-2 border-b-2 border-double border-gray-300 sticky z-10 top-0 bg-white">
    <div class="h-16 flex items-center font-semibold text-gray-400 ">
      <%= link_to '專案內容', project_path(@project), class:" mx-5"%>
      <%= link_to '產品資訊', project_disclosures_path(@project), class:"mx-5"%>
      <%= link_to '常見問題', project_questions_path(@project), class:"comments-link mx-5 "%>
      <%= link_to '留言', project_comments_path(@project), class:"mx-5"%>
    </div>
    <div class="progressBar w-full lg:w-80 ">
      <div class="stepBar w-full lg:w-64 ">
        <div class="bar"></div>
        <div class="bullet"></div>
        <div class="bullet"></div>
        <div class="bullet"></div>
        <div class="bullet"></div>
      </div>
      <div class="flex text-xs justify-between lg:justify-around -mx-4 lg:mx-0">
        <p class="step-text">企劃概念</p>
        <p class="step-text">樣品狀態</p>
        <p class="step-text">生產階段</p>
        <p class="step-text">產品寄送</p>
      </div>
    </div>
  </div>
  <% if @question.errors.any? %>
    <% @question.errors.full_messages.each do |msg| %>
      <%= msg %>
    <% end %>
  <% end %>

  <%= form_with model: @question do |f| %>
    <div>
      <%= f.label :title, "問題主題" %>
      <%= f.text_field :title  %>
    </div>
    <div>
      <%= f.label :content, "問題內容" %>
      <%= f.text_area :content %>
    </div>
    <%= f.submit '送出' %>
  <% end %>
</main>