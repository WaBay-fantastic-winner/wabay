<main id='searchPage' class='bg-gray-100'>
      <%= javascript_pack_tag 'SearchPage' %>
</main>


<h1 class="text-4xl font-bold py-5" >這裡是我的提案清單</h1>

<table>
  <% if @projects.count > 0 %>
    <tr>
      <td class="py-1 px-6"> 提案名稱 </td>
      <td class="py-1 px-6"> 目標金額（新台幣） </td>
      <td class="py-1 px-6"> 計畫發起人 </td>
    </tr>
  <% else %>
    <div>目前還沒有任何專案</div>
  <% end %>
  <% @projects.each do |project| %>
  <tr>
    <td class="py-1 px-6"><%= link_to project.project_title, project_path(project.id) %></a></td>
    <td class="py-1 px-6"><%= project.project_amount_target %></td>
    <td class="py-1 px-6"><%= project.organizer %></td>
  </tr>
  <% end %>
</table>

<div class="font-bold py-5" >
  <%= link_to "前往提案!!", new_project_path %> 
</div>