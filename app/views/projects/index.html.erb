<h1 class="text-4xl font-bold" >這裡是我的提案清單</h1>

<table>
  <tr>
    <td> 提案名稱 </td>
    <td> 目標金額（新台幣） </td>
    <td> 計畫發起人 </td>
  </tr>
<% @projects.each do |project| %>
  <tr>
    <td><%= link_to project.project_title, project_path(project.id) %></a></td>
    <td><%= project.project_amount_target %></td>
    <td><%= project.organizer %></td>
    
  </tr>
<% end %>
</table>

<div class=font-bold >
  <%= link_to "前往提案!!", new_project_path %> 
</div>