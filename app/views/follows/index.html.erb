<h1 class="m-auto p-10 text-xl text-center"><strong>我 的 追 蹤 清 單</strong></h1>

<% if @follows.empty? %> 
  <p class="text-center text-purple-400">您目前還沒有任何追蹤的專案唷～</p>
  <% else %>
    <div class="mx-auto max-w-6xl md:flex">
    <% @follows.each do |follow| %>
      <div class="FundraisingItem mx-5 md:w-1/3 pb-6">
        <img class="w-full rounded-3xl" src="https://picsum.photos/300/200/?random=10" alt="ss"/>
        <div class='mb-7'>
            <h3 class='hover:text-blue-400 my-1 font-semibold text-xl'><%= link_to Project.find_by!(id: follow.followable_id).title, project_path(id: follow.followable_id) %></h3>
            <p class='my-1'>提案者 <span class='text-blue-400 hover:text-blue-300'><%= Project.find_by!(id: follow.followable_id).organizer %></p>
        </div>
        <div>
            <span class='my-1'>目前資金：NT$  <%= Project.find_by!(id: follow.followable_id).current_total %></span>
        </div>
        <div class='mt-3'>
          <button class="col-span-1 border border-gray-400 rounded-lg py-2 hover:bg-blue-400 hover:text-white text-gray-400 duration-200 w-full" data-controller='follow' data-action='click->follow#cancelFollow' data-project-id='<%= Project.find_by!(id: follow.followable_id).id %>'>
            <i class="fa-solid fa-sheet-plastic mr-1"></i>
            取消追蹤
          </button>
        </div> 
      </div>
    <% end %>
  </div>
<% end %>